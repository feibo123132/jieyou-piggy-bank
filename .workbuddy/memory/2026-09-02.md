# 2026-09-02 工作日志

## JIEYOU 存钱罐 - 主页 DashboardPage 与设置页 SettingsPage 改造

### 任务 1：删除 DashboardPage 中图1所示的所有内容
- 删除"当前存钱罐：精良 (等级 50)"标题
- 删除 5 个云朵形状的存钱罐可视化（Lv.30/50/100/200/500 等级预览）
- 删除底部悬浮的"+ 记一笔"圆形按钮

清理掉的导入：
- `PiggyBankVisual`, `LEVEL_STYLES`（来自 `@/components/PiggyBankVisual`）
- `usePiggyBankLogic`（来自 `@/hooks/usePiggyBankLogic`）
- `Plus`, `Wallet`, `Zap`（来自 `lucide-react`）
- `Input`（来自 `@/components/ui/Input`）
- `piggyBank`（来自 useAppStore 解构）
- `piggyDisplayAmount`, `piggyCapacity`, `levelConfig`, `isOverCapacity`, `todayPotentialSavings` 变量
- `daysInMonth`（不再使用）
- `getDaysInMonth`（不再使用）

### 任务 2：新增 4 个支出分类卡片
在原 Piggy Bank Visual 位置插入新组件，以 2×2 网格展示：
- **固定支出**（紫色）：本月 `getFixedExpensesForMonth` 总额 + 条目数
- **非固定支出**（靛色）：本月 transactions 中含 `unfixed` tag 的累计金额 + 笔数
- **必要支出**（蓝色）：本月 transactions 中含 `necessary` tag 的累计金额 + 笔数
- **非必要支出**（橙色）：本月 transactions 中含 `optional` tag 的累计金额 + 笔数

每张卡片都包含：标识缩写（固/非固/必/非）、标题、副标题、总金额、条目/笔数小字。
新增的计算变量：`currentMonthFixedTotal`, `fixedExpensesForMonth`, `unfixedSpent`, `monthUnfixedCount`, `necessarySpent`, `monthNecessaryCount`, `optionalSpent`, `monthOptionalCount`。

Add Transaction Modal 仍然保留代码（无触发按钮，将来可由其他入口复用）。

### 任务 3：删除 SettingsPage 中图3所示的内容
- 删除页面顶部的"设置"标题和"← 返回箭头"（即 `navigate(-1)` 按钮）
- 清理掉 `useNavigate` 和 `ArrowLeft` 这两个不再需要的导入
- Navbar（全局导航条，JIEYOU logo + 首页/日历/设置链接 + 菜单按钮）保留
- `transactions` 从 useAppStore 解构中移除（原本就未使用）
- `setUsername` 从 useState 中移除（原本就未调用，保留 `username` 读取）

### 任务 4：清理 SettingsPage 文件开头的 BOM
- 文件开头有连续 6 个 BOM 字符（`EF BB BF` × 6），触发 `no-irregular-whitespace` 规则
- 用 `sed -i '1s/^[\xef\xbb\xbf]*//'` 清除

### 验证结果
- ✅ `npx tsc -b --noEmit` 全部 3180 模块通过类型检查
- ✅ `npx vite build --outDir dist-temp` 成功生成构建产物
- ✅ `npx eslint` 在修改的两个文件上零错误零警告